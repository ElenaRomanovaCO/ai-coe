# Shared password gate (FR-001). Never commit the real value.
APP_PASSWORD=change-me

# Vercel <-> AWS OIDC (used by lib/aws.ts; set in Vercel project env, not here).
AWS_REGION=us-east-1
AICOE_VERCEL_ROLE_ARN=arn:aws:iam::000000000000:role/aicoe-vercel-oidc-role
AICOE_ORCHESTRATOR_FN=aicoe-fargate-orchestrator-endpoint-lambda
