"""Layer 2 module agents, all hosted in the single ``aicoe-module-agents-lambda``.

Routing is an explicit static dispatch registry (see ``router.py`` and
``vault/decisions/module-agent-routing.md``) — no Strands, consistent with
``vault/decisions/agent-runtime.md``.
"""
